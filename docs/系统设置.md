# 系统设置 - 动态表单展示

在系统中添加配置时，有时可能会需要使用动态显示某个配置，那么可以使用下面的方法。

> 在需要动态显示的表单扩展属性中，增加`data-condition="x字段:1"`
>
> `x字段`表示能够主动触发的表单元素字段，比如：有 2 个表单项，`网站状态`, `关站提示`。
>
> 我希望`关站提示`一开始是隐藏的，当选择`网站状态`为关闭按钮时才会显示出来，那么就可以在`关站提示`这个表单项中增加`data-condition="close_site:0"`
