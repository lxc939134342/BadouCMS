# 首页模式

首页模板为 `template/cms/<主题>/index.html`。常见结构：幻灯片 → 栏目导航 → 内容区块（产品/新闻/案例）→ 友情链接。

关键标签：`slide`（`gid` 必填）、`sort` + `list`（按栏目分组输出内容）、`link`。

参考默认实现：`template/cms/default/index.html`、`examples/homepage.html`。
