# 内容标签：`tags`

原始来源：`tags.md`。可按 `scode` 或 `id` 读取标签，`num` 限制数量。旧模板里的 `limit` 不被当前实现转发；限制数量应写 `num`。字段：`n`、`i`、`link`、`text`。

`target` 仅支持 `tag`，用于链接至独立 `tags.html` 模板。用标签显示相似内容时，使用 `list` 的 `tags='x,y'` 参数；多个标签为任一命中。
